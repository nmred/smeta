系统模块：

1. 数据采集处理中心
mond client -> mond 客户端 ， 负责各个客户端服务器的数据采集，如果是 snmp协议数据直接获取，如果是用户自定义的数据暂时存放在 sqlite 队列中。
mond server -> mond 服务端 ， 将客户端的数据收集到数据中心， 如果是snmp协议数据直接获取，如果是用户自定义的数据从 sqlite 队列中获取。

data 中心

	参数定义中心
		数据源定义
		采集模式定义

